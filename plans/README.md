# Animation improvement plans

Plans in this directory are self-contained handoffs generated by the `improve-animations` workflow. Application source remains unchanged until a plan is explicitly executed.

| # | Plan | Severity | Status | Dependencies |
| --- | --- | --- | --- | --- |
| 001 | [Standardize modal entrances and exits](./001-standardize-modal-entrances-and-exits.md) | MEDIUM | DONE | None |

## Recommended execution order

1. Execute plan 001. It introduces the shared modal-motion primitive and migrates every in-scope modal in one coordinated change.

There are no inter-plan dependencies. Within plan 001, complete the shared tokens and primitive before migrating callers; migrate `ImportCompletionOverlay` last because it depends on the primitive's `onExited` contract.
